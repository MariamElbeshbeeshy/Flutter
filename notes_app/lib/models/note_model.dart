class NoteModel {
  String title;
  String content;
  DateTime createdAt;
  final int color;

  NoteModel({required this.title, required this.content, required this.color, required this.createdAt});
}
